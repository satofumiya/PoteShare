<div class="search_form">
  <%= form_with url: searches_path, local: true, method: :get do |f| %>
    <%= f.text_field :word ,placeholder: item%>
    <%= hidden_field_tag :columns, columns %>
    <% f.submit %>
  <% end %>
</div>
